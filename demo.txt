--// FLUENT UI

local Fluent = loadstring(game:HttpGet(
"https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"
))()

--// WINDOW

local Window = Fluent:CreateWindow({
    Title = "PDZ Hub [PREMIUM]",
    SubTitle = "all script [PREMIUM]",
    TabWidth = 160,
    Size = UDim2.fromOffset(450, 330),
    Acrylic = false,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.RightControl
})

--// ROUND TOGGLE UI BUTTON

local ToggleGui = Instance.new("ScreenGui")
ToggleGui.Name = "ToggleGui"
ToggleGui.Parent = game.CoreGui

local ToggleButton = Instance.new("ImageButton")
ToggleButton.Parent = ToggleGui

-- size nút
ToggleButton.Size = UDim2.new(0,50,0,50)

-- vị trí
ToggleButton.Position = UDim2.new(0,15,0.5,-25)

-- ảnh
ToggleButton.Image = "rbxassetid://3864411488359387"
-- thay ID ảnh tại đây

-- nền trong suốt
ToggleButton.BackgroundColor3 = Color3.fromRGB(255,255,255)
ToggleButton.BackgroundTransparency = 0

-- bo tròn
local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(1,0)
Corner.Parent = ToggleButton

-- viền
local Stroke = Instance.new("UIStroke")
Stroke.Parent = ToggleButton
Stroke.Thickness = 2
Stroke.Color = Color3.fromRGB(0,170,255)

-- kéo nút
ToggleButton.Active = true
ToggleButton.Draggable = true

-- bật/tắt ui
local Visible = true

ToggleButton.MouseButton1Click:Connect(function()

    Visible = not Visible

    Window.Root.Visible = Visible

end)


--// =========================
--// MAIN TAB
--// =========================

local MainTab = Window:AddTab({
    Title = "Main",
    Icon = "home"
})

MainTab:AddSection("BANANA CAT HUB")

MainTab:AddButton({
    Title = "BANANA",
    Description = "main"
    Callback = function()
        repeat wait() until game:IsLoaded() and game.Players.LocalPlayer 
getgenv().Key = "DÁN KEY VÀO" 
loadstring(game:HttpGet("https://raw.githubusercontent.com/obiiyeuem/vthangsitink/main/BananaHub.lua"))()
    end
})

MainTab:AddSection("MARU HUB")

MainTab:AddButton({
    Title = "MARU",
    Description = " main"
    Callback = function()
        
    end
})

MainTab:AddSection("NIGHT HUB")

MainTab:AddButton({
    Title = "Night",
    Description = "main",
    Callback = function()
        repeat wait() until game:IsLoaded()
script_key = "DÁN KEY VÀO"
loadstring(game:HttpGet("https://raw.githubusercontent.com/WhiteX1208/Scripts/refs/heads/main/BF-Beta.lua"))()
    end
})

--// =========================
--// TAB
--// =========================

local StackTab = Window:AddTab({
    Title = "KAITUN",
    Icon = "layers"
})

StackTab:AddSection("BANANA")

StackTab:AddButton({
    Title = "BANANA CAT",
    Callback = function()
        loadstring(game:HttpGet(
        "https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"
        ))()
    end
})

--// =========================
--// TAB
--// =========================

local ItemsTab = Window:AddTab({
    Title = "AUTO BOUNTY",
    Icon = "swords"
})

ItemsTab:AddSection("MARU")

ItemsTab:AddButton({
    Title = "M1",
    Callback = function()
        print("Items")
    end
})

--// =========================
--// TAB
--// =========================

local FruitsTab = Window:AddTab({
    Title = "KAITUN V4",
    Icon = "apple"
})

FruitsTab:AddSection("BANANA")

FruitsTab:AddButton({
    Title = "BANANA",
    Callback = function()
        print("Fruits")
    end
})

--// =========================
--// TAB
--// =========================

local DungeonTab = Window:AddTab({
    Title = "KAITUN LEVI",
    Icon = "shield"
})

DungeonTab:AddSection("BANANA")

DungeonTab:AddButton({
    Title = "LEVI",
    Callback = function()
        print("Dungeon")
    end
})


--// =========================
--// SETTINGS TAB
--// =========================

local SettingsTab = Window:AddTab({
    Title = "Themes",
    Icon = "palette"
})

SettingsTab:AddSection("UI Themes")

--// RED THEME

SettingsTab:AddButton({
    Title = "🔴 Red Theme",
    Callback = function()

        for _,v in pairs(Window.Root:GetDescendants()) do
            if v:IsA("Frame") then
                v.BackgroundColor3 = Color3.fromRGB(170,0,0)
            end
        end

    end
})

--// BLUE THEME

SettingsTab:AddButton({
    Title = "🔵 Blue Theme",
    Callback = function()

        for _,v in pairs(Window.Root:GetDescendants()) do
            if v:IsA("Frame") then
                v.BackgroundColor3 = Color3.fromRGB(0,85,255)
            end
        end

    end
})

--// GREEN THEME

SettingsTab:AddButton({
    Title = "🟢 Green Theme",
    Callback = function()

        for _,v in pairs(Window.Root:GetDescendants()) do
            if v:IsA("Frame") then
                v.BackgroundColor3 = Color3.fromRGB(0,170,0)
            end
        end

    end
})

--// PURPLE THEME

SettingsTab:AddButton({
    Title = "🟣 Purple Theme",
    Callback = function()

        for _,v in pairs(Window.Root:GetDescendants()) do
            if v:IsA("Frame") then
                v.BackgroundColor3 = Color3.fromRGB(85,0,127)
            end
        end

    end
})

--// RED NIGHT THEME

local RedTheme = {
    Background = Color3.fromRGB(120, 15, 15),
    Secondary = Color3.fromRGB(95, 10, 10),
    Accent = Color3.fromRGB(255, 170, 0),
    Text = Color3.fromRGB(255,255,255)
}

for _,v in pairs(Window.Root:GetDescendants()) do

    -- frame
    if v:IsA("Frame") then
        v.BackgroundColor3 = RedTheme.Background
    end

    -- scrolling
    if v:IsA("ScrollingFrame") then
        v.BackgroundColor3 = RedTheme.Secondary
    end

    -- text
    if v:IsA("TextLabel")
    or v:IsA("TextButton") then

        v.TextColor3 = RedTheme.Text

        -- chữ vàng
        if string.find(v.Text:lower(),"information")
        or string.find(v.Text:lower(),"shop")
        or string.find(v.Text:lower(),"stack") then

            v.TextColor3 = RedTheme.Accent
        end
    end

    -- stroke
    if v:IsA("UIStroke") then
        v.Color = Color3.fromRGB(255,140,0)
    end
end

-- nền acrylic đỏ
Window.Root.BackgroundTransparency = 0.15

--// ANTI AFK (luôn để code này ở cuối) 

local Players = game:GetService("Players")
local VirtualUser = game:GetService("VirtualUser")

Players.LocalPlayer.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
    task.wait(1)
    VirtualUser:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
end)
