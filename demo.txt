--// FLUENT UI

local Fluent = loadstring(game:HttpGet(
"https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"
))()

--// WINDOW

local Window = Fluent:CreateWindow({
    Title = "Night Hub [PREMIUM]",
    SubTitle = "@_luongminhnghia - discord.gg/nighthub",
    TabWidth = 160,
    Size = UDim2.fromOffset(500, 330),
    Acrylic = false,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.RightControl
})

--// ROUND TOGGLE UI BUTTON

local ToggleGui = Instance.new("ScreenGui")
ToggleGui.Name = "ToggleGui"
ToggleGui.Parent = game.CoreGui

local ToggleButton = Instance.new("ImageButton")
ToggleButton.Parent = ToggleGui

-- size nút
ToggleButton.Size = UDim2.new(0,50,0,50)

-- vị trí
ToggleButton.Position = UDim2.new(0,15,0.5,-25)

-- ảnh
ToggleButton.Image = "rbxassetid://3864411488359387"
-- thay ID ảnh tại đây

-- nền trong suốt
ToggleButton.BackgroundColor3 = Color3.fromRGB(255,255,255)
ToggleButton.BackgroundTransparency = 0

-- bo tròn
local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(1,0)
Corner.Parent = ToggleButton

-- viền
local Stroke = Instance.new("UIStroke")
Stroke.Parent = ToggleButton
Stroke.Thickness = 2
Stroke.Color = Color3.fromRGB(0,170,255)

-- kéo nút
ToggleButton.Active = true
ToggleButton.Draggable = true

-- bật/tắt ui
local Visible = true

ToggleButton.MouseButton1Click:Connect(function()

    Visible = not Visible

    Window.Root.Visible = Visible

end)


--// =========================
--// MAIN TAB
--// =========================

local MainTab = Window:AddTab({
    Title = "Main",
    Icon = "home"
})

MainTab:AddSection("Main Seperator")

MainTab:AddToggle("Toggle1", {
    Title = "Toggle without desc but enable",
    Default = true,
    Callback = function(v)
        print(v)
    end
})

MainTab:AddToggle("Toggle2", {
    Title = "Toggle",
    Description = "This is toggle with description",
    Default = false,
    Callback = function(v)
        print(v)
    end
})

MainTab:AddSection("Other Features")

MainTab:AddButton({
    Title = "Button",
    Description = "This is a button with description and rich text like: PREMIUM",
    Callback = function()
        print("Button")
    end
})

MainTab:AddDropdown("Dropdown1", {
    Title = "Dropdown without description",
    Values = {
        "Option 1",
        "Option 2",
        "Option 3"
    },
    Multi = false,
    Default = 1
})

MainTab:AddDropdown("Dropdown2", {
    Title = "Dropdown",
    Description = "Select Any Values in Dropdown",
    Values = {
        "Option 1",
        "Option 2",
        "Option 3"
    },
    Multi = false,
    Default = 1
})

--// =========================
--// STACK TAB
--// =========================

local StackTab = Window:AddTab({
    Title = "Stack",
    Icon = "layers"
})

StackTab:AddSection("Scripts")

StackTab:AddButton({
    Title = "Infinite Yield",
    Callback = function()
        loadstring(game:HttpGet(
        "https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source"
        ))()
    end
})

--// =========================
--// ITEMS TAB
--// =========================

local ItemsTab = Window:AddTab({
    Title = "Items",
    Icon = "swords"
})

ItemsTab:AddSection("Items")

ItemsTab:AddButton({
    Title = "Get Items",
    Callback = function()
        print("Items")
    end
})

--// =========================
--// FRUITS TAB
--// =========================

local FruitsTab = Window:AddTab({
    Title = "Fruits",
    Icon = "apple"
})

FruitsTab:AddSection("Fruits")

FruitsTab:AddButton({
    Title = "Auto Fruits",
    Callback = function()
        print("Fruits")
    end
})

--// =========================
--// DUNGEON TAB
--// =========================

local DungeonTab = Window:AddTab({
    Title = "Dungeon",
    Icon = "shield"
})

DungeonTab:AddSection("Dungeon")

DungeonTab:AddButton({
    Title = "Auto Dungeon",
    Callback = function()
        print("Dungeon")
    end
})

--// =========================
--// LOCALPLAYER TAB
--// =========================

local PlayerTab = Window:AddTab({
    Title = "LocalPlayer",
    Icon = "user"
})

PlayerTab:AddSection("Player")

PlayerTab:AddSlider("WS", {
    Title = "WalkSpeed",
    Default = 16,
    Min = 16,
    Max = 200,
    Rounding = 0,
    Callback = function(v)
        game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = v
    end
})

--// =========================
--// SETTINGS TAB
--// =========================

local SettingsTab = Window:AddTab({
    Title = "Themes",
    Icon = "palette"
})

SettingsTab:AddSection("UI Themes")

--// RED THEME

SettingsTab:AddButton({
    Title = "🔴 Red Theme",
    Callback = function()

        for _,v in pairs(Window.Root:GetDescendants()) do
            if v:IsA("Frame") then
                v.BackgroundColor3 = Color3.fromRGB(170,0,0)
            end
        end

    end
})

--// BLUE THEME

SettingsTab:AddButton({
    Title = "🔵 Blue Theme",
    Callback = function()

        for _,v in pairs(Window.Root:GetDescendants()) do
            if v:IsA("Frame") then
                v.BackgroundColor3 = Color3.fromRGB(0,85,255)
            end
        end

    end
})

--// GREEN THEME

SettingsTab:AddButton({
    Title = "🟢 Green Theme",
    Callback = function()

        for _,v in pairs(Window.Root:GetDescendants()) do
            if v:IsA("Frame") then
                v.BackgroundColor3 = Color3.fromRGB(0,170,0)
            end
        end

    end
})

--// PURPLE THEME

SettingsTab:AddButton({
    Title = "🟣 Purple Theme",
    Callback = function()

        for _,v in pairs(Window.Root:GetDescendants()) do
            if v:IsA("Frame") then
                v.BackgroundColor3 = Color3.fromRGB(85,0,127)
            end
        end

    end
})

--// VIETNAM THEME

SettingsTab:AddButton({
    Title = "🇻🇳 Vietnam Theme",
    Callback = function()

        for _,v in pairs(Window.Root:GetDescendants()) do
            if v:IsA("Frame") then
                v.BackgroundColor3 = Color3.fromRGB(180,0,0)
            end
        end

        if Window.Root:FindFirstChild("VietnamStar") then
            Window.Root.VietnamStar:Destroy()
        end

        local Star = Instance.new("TextLabel")
        Star.Name = "VietnamStar"
        Star.Parent = Window.Root

        Star.Size = UDim2.new(0,150,0,150)
        Star.Position = UDim2.new(0.5,-75,0.5,-75)

        Star.BackgroundTransparency = 1
        Star.Text = "✦"
        Star.TextScaled = true
        Star.Font = Enum.Font.GothamBold

        Star.TextColor3 = Color3.fromRGB(255,221,0)
        Star.TextTransparency = 0.75

    end
})

--// NOTIFY

Fluent:Notify({
    Title = "Night Hub",
    Content = "UI Loaded Successfully",
    Duration = 5
})



--// LOW GRAPHICS

BoostTab:AddButton({
    Title = "Low Graphics",
    Description = "Reduce graphics & effects",
    Callback = function()

        local Lighting = game:GetService("Lighting")
        local Terrain = workspace:FindFirstChildOfClass("Terrain")

        pcall(function()

            settings().Rendering.QualityLevel = Enum.QualityLevel.Level01

            Lighting.GlobalShadows = false
            Lighting.FogEnd = 100000
            Lighting.Brightness = 1

            if Terrain then
                Terrain.WaterWaveSize = 0
                Terrain.WaterWaveSpeed = 0
                Terrain.WaterReflectance = 0
                Terrain.WaterTransparency = 0.2
            end

            for _,v in ipairs(game:GetDescendants()) do

                if v:IsA("BasePart") then
                    v.Material = Enum.Material.Plastic
                    v.Reflectance = 0
                end

                if v:IsA("Decal") or v:IsA("Texture") then
                    v.Transparency = 0.4
                end

                if v:IsA("ParticleEmitter") then
                    v.Rate = 0
                end

                if v:IsA("Trail")
                or v:IsA("Smoke")
                or v:IsA("Fire")
                or v:IsA("Sparkles") then
                    v.Enabled = false
                end
            end

            for _,v in ipairs(Lighting:GetDescendants()) do

                if v:IsA("BloomEffect")
                or v:IsA("BlurEffect")
                or v:IsA("SunRaysEffect")
                or v:IsA("DepthOfFieldEffect") then
                    v.Enabled = false
                end
            end

        end)

        Fluent:Notify({
            Title = "Boost",
            Content = "Low Graphics Enabled",
            Duration = 3
        })

    end
})

--// FPS BOOST

BoostTab:AddButton({
    Title = "FPS Boost",
    Description = "Boost FPS for mobile",
    Callback = function()

        pcall(function()

            settings().Rendering.QualityLevel =
                Enum.QualityLevel.Level01

            game:GetService("RunService"):Set3dRenderingEnabled(true)

            for _,v in ipairs(game:GetDescendants()) do

                if v:IsA("ParticleEmitter")
                or v:IsA("PostEffect") then
                    v.Enabled = false
                end
            end

        end)

        Fluent:Notify({
            Title = "Boost",
            Content = "FPS Boost Enabled",
            Duration = 3
        })

    end
})

--// REMOVE TEXTURES

BoostTab:AddButton({
    Title = "Remove Textures",
    Description = "Ultra FPS mode",
    Callback = function()

        for _,v in ipairs(game:GetDescendants()) do

            if v:IsA("Texture")
            or v:IsA("Decal") then
                v:Destroy()
            end
        end

        Fluent:Notify({
            Title = "Boost",
            Content = "Textures Removed",
            Duration = 3
        })

    end
})

--// DARK GRAPHICS

BoostTab:AddButton({
    Title = "Dark Graphics",
    Description = "Dark optimized graphics",
    Callback = function()

        local Lighting = game:GetService("Lighting")

        Lighting.Brightness = 0.8

        for _,v in ipairs(Lighting:GetDescendants()) do

            if v:IsA("ColorCorrectionEffect") then
                v.Saturation = -0.2
                v.Brightness = -0.05
            end
        end

        Fluent:Notify({
            Title = "Boost",
            Content = "Dark Graphics Enabled",
            Duration = 3
        })

    end
})

Window:SelectTab(MainTab)

--// ANTI AFK (luôn để code này ở cuối) 

local Players = game:GetService("Players")
local VirtualUser = game:GetService("VirtualUser")

Players.LocalPlayer.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
    task.wait(1)
    VirtualUser:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
end)
