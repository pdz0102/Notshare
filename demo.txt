--// UI LIBRARY
local Fluent = loadstring(game:HttpGet(
"https://raw.githubusercontent.com/dawid-scripts/Fluent/master/source.lua"
))()

--// WINDOW
local Window = Fluent:CreateWindow({
    Title = "PDZ Hub [PREMIUM]",
    SubTitle = "PREMIUM SCRIPT",
    TabWidth = 160,
    Size = UDim2.fromOffset(460,330),
    Acrylic = false,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.RightControl
})

--// TOGGLE BUTTON

local ToggleGui = Instance.new("ScreenGui")
ToggleGui.Name = "ToggleGui"
ToggleGui.Parent = gethui and gethui() or game.CoreGui
ToggleGui.ResetOnSpawn = false

local ToggleButton = Instance.new("ImageButton")
ToggleButton.Parent = ToggleGui

ToggleButton.Size = UDim2.new(0,45,0,45)
ToggleButton.Position = UDim2.new(0,15,0.5,-25)

ToggleButton.Image = "rbxassetid://89311262811917"

ToggleButton.BackgroundColor3 = Color3.fromRGB(255,255,255)

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(1,0)
UICorner.Parent = ToggleButton

local Stroke = Instance.new("UIStroke")
Stroke.Parent = ToggleButton
Stroke.Thickness = 2
Stroke.Color = Color3.fromRGB(0,170,255)

ToggleButton.Active = true
ToggleButton.Draggable = true

local UiOpen = true

ToggleButton.MouseButton1Click:Connect(function()

    UiOpen = not UiOpen

    for _,v in pairs(game.CoreGui:GetChildren()) do
        if v.Name == "Fluent" then
            v.Enabled = UiOpen
        end
    end

end)

--// MAIN TAB

local MainTab = Window:AddTab({
    Title = "Main",
    Icon = "home"
})

MainTab:AddSection("Execute Script")

MainTab:AddButton({
    Title = "Execute Script 1",
    Description = "Run script",
    Callback = function()

        loadstring(game:HttpGet("LINK_SCRIPT"))()

    end
})

MainTab:AddButton({
    Title = "Execute Script 2",
    Description = "Run script",
    Callback = function()

        print("Executed")

    end
})

--// THEME TAB

local ThemeTab = Window:AddTab({
    Title = "Themes",
    Icon = "palette"
})

ThemeTab:AddSection("UI Colors")

-- XANH DƯƠNG
ThemeTab:AddButton({
    Title = "Blue Theme",
    Callback = function()

        for _,v in pairs(Window.Root:GetDescendants()) do
            if v:IsA("Frame") then
                v.BackgroundColor3 = Color3.fromRGB(0,85,255)
            end
        end

    end
})

-- TÍM
ThemeTab:AddButton({
    Title = "Purple Theme",
    Callback = function()

        for _,v in pairs(Window.Root:GetDescendants()) do
            if v:IsA("Frame") then
                v.BackgroundColor3 = Color3.fromRGB(170,0,255)
            end
        end

    end
})

-- XANH LÁ
ThemeTab:AddButton({
    Title = "Green Theme",
    Callback = function()

        for _,v in pairs(Window.Root:GetDescendants()) do
            if v:IsA("Frame") then
                v.BackgroundColor3 = Color3.fromRGB(0,170,0)
            end
        end

    end
})

-- ĐỎ
ThemeTab:AddButton({
    Title = "Red Theme",
    Callback = function()

        for _,v in pairs(Window.Root:GetDescendants()) do
            if v:IsA("Frame") then
                v.BackgroundColor3 = Color3.fromRGB(170,0,0)
            end
        end

    end
})

-- ĐEN
ThemeTab:AddButton({
    Title = "Black Theme",
    Callback = function()

        for _,v in pairs(Window.Root:GetDescendants()) do
            if v:IsA("Frame") then
                v.BackgroundColor3 = Color3.fromRGB(25,25,25)
            end
        end

    end
})

-- TRẮNG
ThemeTab:AddButton({
    Title = "White Theme",
    Callback = function()

        for _,v in pairs(Window.Root:GetDescendants()) do
            if v:IsA("Frame") then
                v.BackgroundColor3 = Color3.fromRGB(255,255,255)
            end
        end

    end
})

--// SETTINGS TAB

local SettingsTab = Window:AddTab({
    Title = "Settings",
    Icon = "settings"
})

SettingsTab:AddSection("Performance")

--// FPS BOOST

SettingsTab:AddButton({
    Title = "FPS Boost",
    Description = "Boost FPS & Reduce Ping",
    Callback = function()

        local lighting = game:GetService("Lighting")

        lighting.GlobalShadows = false
        lighting.FogEnd = 9e9
        lighting.Brightness = 0

        settings().Rendering.QualityLevel = Enum.QualityLevel.Level01

        for i,v in pairs(game:GetDescendants()) do

            if v:IsA("Part")
            or v:IsA("Union")
            or v:IsA("MeshPart") then

                v.Material = Enum.Material.Plastic
                v.Reflectance = 0

            end

            if v:IsA("Decal")
            or v:IsA("Texture") then

                v.Transparency = 1

            end

            if v:IsA("ParticleEmitter")
            or v:IsA("Trail") then

                v.Enabled = false

            end

        end

    end
})

--// ANTI LAG

SettingsTab:AddButton({
    Title = "Anti Lag",
    Description = "Remove unnecessary effects",
    Callback = function()

        for _,v in pairs(game:GetDescendants()) do

            if v:IsA("Explosion")
            or v:IsA("Fire")
            or v:IsA("Smoke")
            or v:IsA("Sparkles") then

                v:Destroy()

            end

        end

    end
})

--// LOW GRAPHICS

SettingsTab:AddButton({
    Title = "Low Graphics",
    Description = "Dark & Smooth Graphics",
    Callback = function()

        local lighting = game:GetService("Lighting")

        lighting.Brightness = 1
        lighting.GlobalShadows = false
        lighting.FogEnd = 500
        lighting.OutdoorAmbient = Color3.fromRGB(80,80,80)

        settings().Rendering.QualityLevel = Enum.QualityLevel.Level01

    end
})

--// REMOVE TEXTURE

SettingsTab:AddButton({
    Title = "Remove Texture",
    Description = "Delete all textures",
    Callback = function()

        for _,v in pairs(game:GetDescendants()) do

            if v:IsA("Texture")
            or v:IsA("Decal") then

                v:Destroy()

            end

        end

    end
})

--// ANTI AFK (luôn để code này ở cuối) 

local Players = game:GetService("Players")
local VirtualUser = game:GetService("VirtualUser")

Players.LocalPlayer.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
    task.wait(1)
    VirtualUser:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
end)

Fluent:Notify({
    Title = "PREMIUM IS COMING",
    Content = "PREMIUM HUB Loaded",
    Duration = 5
})
