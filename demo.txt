--// BANANA CAT HUB MOBILE UI
--// FULL REMAKE
--// ROBLOX LUA

repeat wait() until game:IsLoaded()

--====================================================
-- SERVICES
--====================================================

local TweenService = game:GetService("TweenService")
local UserInputService = game:GetService("UserInputService")
local Players = game:GetService("Players")

local Player = Players.LocalPlayer

--====================================================
-- REMOVE OLD
--====================================================

pcall(function()
	game.CoreGui.WaterCathub:Destroy()
end)

--====================================================
-- GUI
--====================================================

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "WaterCathub"
ScreenGui.Parent = game.CoreGui
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

--====================================================
-- TOGGLE BUTTON
--====================================================

local ToggleButton = Instance.new("ImageButton")
ToggleButton.Parent = ScreenGui
ToggleButton.Size = UDim2.new(0,55,0,55)
ToggleButton.Position = UDim2.new(0,15,0.5,-32)
ToggleButton.BackgroundTransparency = 1
ToggleButton.Image = "rbxassetid://10734950309"

Instance.new("UICorner", ToggleButton).CornerRadius = UDim.new(1,0)

local ToggleStroke = Instance.new("UIStroke", ToggleButton)
ToggleStroke.Color = Color3.fromRGB(255,255,255)
ToggleStroke.Thickness = 2

--====================================================
-- MAIN GUI
--====================================================

local Main = Instance.new("Frame")
Main.Parent = ScreenGui
Main.Size = UDim2.new(0,580,0,340)
Main.Position = UDim2.new(0.5,-290,0.5,-170)
Main.BackgroundColor3 = Color3.fromRGB(15,15,15)
Main.BorderSizePixel = 0
Main.Active = true
Main.Draggable = true

Instance.new("UICorner", Main).CornerRadius = UDim.new(0,15)

local MainStroke = Instance.new("UIStroke", Main)
MainStroke.Color = Color3.fromRGB(50,50,50)

--====================================================
-- TITLE
--====================================================

local Title = Instance.new("TextLabel")
Title.Parent = Main
Title.BackgroundTransparency = 1
Title.Position = UDim2.new(0,15,0,5)
Title.Size = UDim2.new(1,0,0,50)

Title.Font = Enum.Font.GothamBold
Title.TextSize = 21
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.RichText = true

Title.Text =
'<font color="rgb(255,210,70)">Water</font> Cat hub'

Title.TextColor3 = Color3.new(1,1,1)

--====================================================
-- SIDEBAR
--====================================================

local Sidebar = Instance.new("Frame")
Sidebar.Parent = Main
Sidebar.Position = UDim2.new(0,10,0,60)
Sidebar.Size = UDim2.new(0,145,1,-70)

Sidebar.BackgroundColor3 = Color3.fromRGB(20,20,20)
Sidebar.BorderSizePixel = 0

Instance.new("UICorner", Sidebar)

local SidebarLayout = Instance.new("UIListLayout", Sidebar)
SidebarLayout.Padding = UDim.new(0,8)
SidebarLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

--====================================================
-- CONTENT
--====================================================

local Content = Instance.new("Frame")
Content.Parent = Main
Content.Position = UDim2.new(0,160,0,60)
Content.Size = UDim2.new(1,-170,1,-70)
Content.BackgroundTransparency = 1

--====================================================
-- CREATE TAB
--====================================================

local function CreateTab(name)

	local Button = Instance.new("TextButton")
	Button.Parent = Sidebar
	Button.Size = UDim2.new(1,-10,0,45)

	Button.BackgroundColor3 = Color3.fromRGB(30,30,30)
	Button.Text = name
	Button.Font = Enum.Font.GothamBold
	Button.TextSize = 17
	Button.TextColor3 = Color3.new(1,1,1)
	Button.BorderSizePixel = 0

	Instance.new("UICorner", Button)

	local Page = Instance.new("ScrollingFrame")
	Page.Parent = Content
	Page.Size = UDim2.new(1,0,1,0)
	Page.BackgroundTransparency = 1
	Page.ScrollBarThickness = 3
	Page.CanvasSize = UDim2.new(0,0,0,0)
	Page.Visible = false

	local Layout = Instance.new("UIListLayout", Page)
	Layout.Padding = UDim.new(0,10)

	Layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()
		Page.CanvasSize = UDim2.new(0,0,0,Layout.AbsoluteContentSize.Y + 15)
	end)

	Button.MouseButton1Click:Connect(function()

		for _,v in pairs(Content:GetChildren()) do
			if v:IsA("ScrollingFrame") then
				v.Visible = false
			end
		end

		Page.Visible = true
	end)

	return Page
end

--====================================================
-- CATEGORY
--====================================================

local function CreateCategory(parent, title)

	local Category = Instance.new("Frame")
	Category.Parent = parent
	Category.Size = UDim2.new(1,-10,0,50)
	Category.BackgroundColor3 = Color3.fromRGB(10,10,10)
	Category.BorderSizePixel = 0
	Category.ClipsDescendants = true

	Instance.new("UICorner", Category)

	local Open = false

	-- TITLE
	local Title = Instance.new("TextButton")
	Title.Parent = Category
	Title.Size = UDim2.new(1,0,0,50)

	Title.BackgroundColor3 = Color3.fromRGB(15,15,15)
	Title.BorderSizePixel = 0

	Title.Text = title
	Title.Font = Enum.Font.GothamBold
	Title.TextSize = 21
	Title.TextColor3 = Color3.new(1,1,1)

	Instance.new("UICorner", Title)

	-- GOLD LINE
	local Line = Instance.new("Frame")
	Line.Parent = Title
	Line.Size = UDim2.new(1,0,0,2)
	Line.Position = UDim2.new(0,0,1,-2)

	Line.BackgroundColor3 = Color3.fromRGB(255,190,60)
	Line.BorderSizePixel = 0

	-- CONTENT
	local Content = Instance.new("Frame")
	Content.Parent = Category
	Content.Position = UDim2.new(0,0,0,55)
	Content.Size = UDim2.new(1,0,0,0)
	Content.BackgroundTransparency = 1

	local Layout = Instance.new("UIListLayout", Content)
	Layout.Padding = UDim.new(0,8)

	Layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(function()

		Content.Size = UDim2.new(
			1,
			0,
			0,
			Layout.AbsoluteContentSize.Y
		)

		if Open then

			Category.Size = UDim2.new(
				1,
				-10,
				0,
				Layout.AbsoluteContentSize.Y + 65
			)
		end
	end)

	Title.MouseButton1Click:Connect(function()

		Open = not Open

		if Open then

			Category:TweenSize(
				UDim2.new(
					1,
					-10,
					0,
					Layout.AbsoluteContentSize.Y + 65
				),
				"Out",
				"Quad",
				0.25,
				true
			)

		else

			Category:TweenSize(
				UDim2.new(1,-10,0,50),
				"Out",
				"Quad",
				0.25,
				true
			)
		end
	end)

	return Content
end

--====================================================
-- BUTTON
--====================================================

local function CreateButton(parent, text, callback)

	local Holder = Instance.new("Frame")
	Holder.Parent = parent
	Holder.Size = UDim2.new(1,0,0,65)

	Holder.BackgroundColor3 = Color3.fromRGB(20,20,20)
	Holder.BorderSizePixel = 0

	Instance.new("UICorner", Holder)

	local Label = Instance.new("TextLabel")
	Label.Parent = Holder
	Label.BackgroundTransparency = 1
	Label.Position = UDim2.new(0,15,0,0)
	Label.Size = UDim2.new(1,-170,1,0)

	Label.Font = Enum.Font.GothamBold
	Label.Text = text
	Label.TextSize = 17
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.TextColor3 = Color3.new(1,1,1)

	local Button = Instance.new("TextButton")
	Button.Parent = Holder
	Button.Size = UDim2.new(0,120,0,42)
	Button.Position = UDim2.new(1,-140,0.5,-21)

	Button.BackgroundColor3 = Color3.fromRGB(230,200,120)
	Button.Text = "Click"

	Button.Font = Enum.Font.GothamBold
	Button.TextSize = 17
	Button.TextColor3 = Color3.fromRGB(40,30,0)

	Button.BorderSizePixel = 0

	Instance.new("UICorner", Button)

	Button.MouseButton1Click:Connect(function()
		pcall(callback)
	end)
end

--====================================================
-- TOGGLE
--====================================================

local function CreateToggle(parent, text, callback)

	local Holder = Instance.new("Frame")
	Holder.Parent = parent
	Holder.Size = UDim2.new(1,0,0,55)

	Holder.BackgroundColor3 = Color3.fromRGB(20,20,20)
	Holder.BorderSizePixel = 0

	Instance.new("UICorner", Holder)

	local Label = Instance.new("TextLabel")
	Label.Parent = Holder
	Label.BackgroundTransparency = 1
	Label.Position = UDim2.new(0,15,0,0)
	Label.Size = UDim2.new(1,-80,1,0)

	Label.Font = Enum.Font.GothamBold
	Label.Text = text
	Label.TextSize = 20
	Label.TextXAlignment = Enum.TextXAlignment.Left
	Label.TextColor3 = Color3.new(1,1,1)

	local Toggle = Instance.new("TextButton")
	Toggle.Parent = Holder
	Toggle.Size = UDim2.new(0,35,0,35)
	Toggle.Position = UDim2.new(1,-50,0.5,-17)

	Toggle.BackgroundColor3 = Color3.fromRGB(25,25,25)
	Toggle.Text = ""
	Toggle.BorderSizePixel = 0

	local Stroke = Instance.new("UIStroke", Toggle)
	Stroke.Color = Color3.fromRGB(255,190,60)
	Stroke.Thickness = 2

	Instance.new("UICorner", Toggle)

	local Enabled = false

	Toggle.MouseButton1Click:Connect(function()

		Enabled = not Enabled

		if Enabled then
			Toggle.BackgroundColor3 = Color3.fromRGB(255,190,60)
		else
			Toggle.BackgroundColor3 = Color3.fromRGB(25,25,25)
		end

		pcall(function()
			callback(Enabled)
		end)
	end)
end

---- tab nhỏ ----

local function CreateSection(parent, title)

	local Section = Instance.new("Frame")
	Section.Parent = parent
	Section.Size = UDim2.new(1,-10,0,45)
	Section.BackgroundColor3 = Color3.fromRGB(8,8,8)
	Section.BorderSizePixel = 0
	Section.ClipsDescendants = true

	Instance.new("UICorner", Section).CornerRadius = UDim.new(0,10)

	-- HEADER
	local Header = Instance.new("TextButton")
	Header.Parent = Section
	Header.Size = UDim2.new(1,0,0,45)

	Header.BackgroundColor3 = Color3.fromRGB(10,10,10)
	Header.BorderSizePixel = 0

	Header.Text = title
	Header.Font = Enum.Font.GothamBold
	Header.TextSize = 9
	Header.TextColor3 = Color3.new(1,1,1)

	Instance.new("UICorner", Header).CornerRadius = UDim.new(0,10)

	-- GOLD LINE
	local Line = Instance.new("Frame")
	Line.Parent = Header
	Line.AnchorPoint = Vector2.new(0.5,1)
	Line.Position = UDim2.new(0.5,0,1,-1)

	Line.Size = UDim2.new(0.92,0,0,2)

	Line.BackgroundColor3 = Color3.fromRGB(255,190,60)
	Line.BorderSizePixel = 0

	-- CONTENT
	local Container = Instance.new("Frame")
	Container.Parent = Section
	Container.Position = UDim2.new(0,0,0,52)
	Container.Size = UDim2.new(1,0,0,0)
	Container.BackgroundTransparency = 1

	local Layout = Instance.new("UIListLayout")
	Layout.Parent = Container
	Layout.Padding = UDim.new(0,8)

	local Open = false

	local function UpdateSize()

		local Y = Layout.AbsoluteContentSize.Y

		Container.Size = UDim2.new(1,0,0,Y)

		if Open then

			Section:TweenSize(
				UDim2.new(1,-10,0,Y + 60),
				"Out",
				"Quad",
				0.25,
				true
			)

		end
	end

	Layout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(UpdateSize)

	Header.MouseButton1Click:Connect(function()

		Open = not Open

		if Open then

			UpdateSize()

		else

			Section:TweenSize(
				UDim2.new(1,-10,0,45),
				"Out",
				"Quad",
				0.25,
				true
			)
		end
	end)

	return Container
end

--====================================================
-- TABS
--====================================================

local MainTab = CreateTab("Main")
local AutoBounty = CreateTab("Auto Bounty")
local LeviTab = CreateTab("Kaitun Levi")
local V4Tab = CreateTab("Kaitun V4")
local OtherTab = CreateTab("Khác")

MainTab.Visible = true

--====================================================
-- MAIN CATEGORY
--====================================================

local MainCategory = CreateCategory(MainTab,"Main Scripts")

CreateButton(MainCategory,"Banana",function()
	loadstring(game:HttpGet("LINK"))()
end)

CreateButton(MainCategory,"Night Hub",function()
	loadstring(game:HttpGet("LINK"))()
end)

CreateButton(MainCategory,"Maru",function()
	loadstring(game:HttpGet("LINK"))()
end)

--====================================================
-- AUTO BOUNTY
--====================================================

local BountyCategory = CreateCategory(AutoBounty,"Auto Bounty")

CreateToggle(BountyCategory,"M1",function(v)
	print(v)
end)

CreateToggle(BountyCategory,"Skills",function(v)
	print(v)
end)

--====================================================
-- LEVI
--====================================================

local LeviCategory = CreateCategory(LeviTab,"Levi Scripts")

CreateButton(LeviCategory,"Night",function()
	loadstring(game:HttpGet("LINK"))()
end)

CreateButton(LeviCategory,"Banana",function()
	loadstring(game:HttpGet("LINK"))()
end)

--====================================================
-- V4
--====================================================

local V4Category = CreateCategory(V4Tab,"V4 Scripts")

CreateButton(V4Category,"Night",function()
	loadstring(game:HttpGet("LINK"))()
end)

CreateButton(V4Category,"Banana",function()
	loadstring(game:HttpGet("LINK"))()
end)

--====================================================
-- OTHER
--====================================================

local OtherCategory = CreateCategory(OtherTab,"Other")

CreateToggle(OtherCategory,"Example Toggle",function(v)
	print(v)
end)



--====================================================
-- TOGGLE GUI
--====================================================

local Opened = true

ToggleButton.MouseButton1Click:Connect(function()

	Opened = not Opened

	Main.Visible = Opened

	if Opened then

		TweenService:Create(
			ToggleButton,
			TweenInfo.new(0.2),
			{Size = UDim2.new(0,50,0,50)}
		):Play()

	else

		TweenService:Create(
			ToggleButton,
			TweenInfo.new(0.2),
			{Size = UDim2.new(0,80,0,80)}
		):Play()

	end
end)

--====================================================
-- DRAG TOGGLE BUTTON
--====================================================

local dragging
local dragInput
local dragStart
local startPos

local function update(input)

	local delta = input.Position - dragStart

	ToggleButton.Position = UDim2.new(
		startPos.X.Scale,
		startPos.X.Offset + delta.X,
		startPos.Y.Scale,
		startPos.Y.Offset + delta.Y
	)
end

ToggleButton.InputBegan:Connect(function(input)

	if input.UserInputType == Enum.UserInputType.Touch
	or input.UserInputType == Enum.UserInputType.MouseButton1 then

		dragging = true
		dragStart = input.Position
		startPos = ToggleButton.Position

		input.Changed:Connect(function()

			if input.UserInputState == Enum.UserInputState.End then
				dragging = false
			end
		end)
	end
end)

ToggleButton.InputChanged:Connect(function(input)

	if input.UserInputType == Enum.UserInputType.Touch
	or input.UserInputType == Enum.UserInputType.MouseMovement then

		dragInput = input
	end
end)

UserInputService.InputChanged:Connect(function(input)

	if input == dragInput and dragging then
		update(input)
	end
end)
