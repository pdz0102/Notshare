local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local SaveManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/SaveManager.lua"))()
local InterfaceManager = loadstring(game:HttpGet("https://raw.githubusercontent.com/dawid-scripts/Fluent/master/Addons/InterfaceManager.lua"))()

local Window = Fluent:CreateWindow({
    Title = "PDZ Hub",
    SubTitle = "not share",
    TabWidth = 160,
    Size = UDim2.fromOffset(450, 350),
    Acrylic = false,
    Theme = "Dark",
    MinimizeKey = Enum.KeyCode.RightControl
})

--// NÚT BẬT/TẮT MENU

local ToggleGui = Instance.new("ScreenGui")
ToggleGui.Name = "ToggleGui"
ToggleGui.Parent = game.CoreGui

local ToggleButton = Instance.new("ImageButton")
ToggleButton.Parent = ToggleGui
ToggleButton.Size = UDim2.new(0,60,0,60)
ToggleButton.Position = UDim2.new(0,10,0.5,-30)

-- ảnh 
ToggleButton.Image = "rbxassetid://95667714826549"
-- thay 0 bằng ID ảnh upload của bạn
-- ID ảnh của gamepass của bạn trên roblox

ToggleButton.BackgroundTransparency = 1
ToggleButton.Active = true
ToggleButton.Draggable = true

-- bo tròn
local Corner = Instance.new("UICorner")
Corner.CornerRadius = UDim.new(1,0)
Corner.Parent = ToggleButton

-- di CHUYỂN
ToggleGui.DisplayOrder = 999999

local Open = true

ToggleButton.MouseButton1Click:Connect(function()
    Open = not Open

    local Gui = Window.Root.Parent
    Gui.Enabled = Open
end)

--// TAB INFORMATION

local InfoTab = Window:AddTab({
    Title = "Information",
    Icon = "info"
})

InfoTab:AddSection("Owner Infomation")

InfoTab:AddParagraph({
    Title = "Information",
    Content = [[
Name: idk
From: VietNamese 🇻🇳
Birthday: idl
Age: idk
Home Adress: idk
]]
})

InfoTab:AddSection("Library Information")

InfoTab:AddParagraph({
    Title = "Library Information",
    Content = [[
Modified Name: FLUENT
Original Author: Dawid
]]
})

InfoTab:AddSection("Discord")

InfoTab:AddButton({
    Title = "Copy Discord Invite Link",
    Description = "discord săn levi",
    Callback = function()
        setclipboard("https://discord.gg/zxYasXreP")
        Fluent:Notify({
            Title = "Copied",
            Content = "Discord link copied",
            Duration = 3
        })
    end
})

InfoTab:AddButton({
    Title = "Set to All Theme",
    Description = "Apply theme settings",
    Callback = function()
        Fluent:Notify({
            Title = "Theme",
            Content = "Theme applied",
            Duration = 3
        })
    end
})

--// NIGHT HUB TAB

local NightTab = Window:AddTab({
    Title = " omg HUB",
    Icon = "moon"
})

--// BUTTON 2
NightTab:AddButton({
    Title = "banana cat",
    Description = "free free",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/tamdznanatv/hubpremumcomback/refs/heads/main/NaNaTVHubcomback.luau"))()
    end
})

--// TAB BANANA

local BananaTab = Window:AddTab({
    Title = "Banana",
    Icon = "banana"
})

BananaTab:AddSection("Banana Hub")

BananaTab:AddButton({
    Title = "fps Script",
    Description = "thôi",
    Callback = function()
        loadstring(game:HttpGet("https://pastefy.app/OruW5AwY/raw"))()
    end
})

--// TAB MARU

local MaruTab = Window:AddTab({
    Title = "Maru",
    Icon = "star"
})

MaruTab:AddSection("Maru Hub")

MaruTab:AddButton({
    Title = "Maru Script",
    Description = "test thôi",
    Callback = function()
        repeat wait() until game:IsLoaded() and game.Players.LocalPlayer 
getgenv().AutoLoad = true --auto load on hop
getgenv().team = "Pirates" -- Pirates or Marines
loadstring(game:HttpGet("https://raw.githubusercontent.com/vinh129150/hack/refs/heads/main/Bloxfruits.lua"))()
    end
})

--// BOOST TAB

local BoostTab = Window:AddTab({
    Title = "Boost",
    Icon = "zap"
})

BoostTab:AddSection("FPS & Graphics")

--// LOW GRAPHICS

BoostTab:AddButton({
    Title = "Low Graphics",
    Description = "Reduce graphics & effects",
    Callback = function()

        local Lighting = game:GetService("Lighting")
        local Terrain = workspace:FindFirstChildOfClass("Terrain")

        pcall(function()

            settings().Rendering.QualityLevel = Enum.QualityLevel.Level01

            Lighting.GlobalShadows = false
            Lighting.FogEnd = 100000
            Lighting.Brightness = 1

            if Terrain then
                Terrain.WaterWaveSize = 0
                Terrain.WaterWaveSpeed = 0
                Terrain.WaterReflectance = 0
                Terrain.WaterTransparency = 0.2
            end

            for _,v in ipairs(game:GetDescendants()) do

                if v:IsA("BasePart") then
                    v.Material = Enum.Material.Plastic
                    v.Reflectance = 0
                end

                if v:IsA("Decal") or v:IsA("Texture") then
                    v.Transparency = 0.4
                end

                if v:IsA("ParticleEmitter") then
                    v.Rate = 0
                end

                if v:IsA("Trail")
                or v:IsA("Smoke")
                or v:IsA("Fire")
                or v:IsA("Sparkles") then
                    v.Enabled = false
                end
            end

            for _,v in ipairs(Lighting:GetDescendants()) do

                if v:IsA("BloomEffect")
                or v:IsA("BlurEffect")
                or v:IsA("SunRaysEffect")
                or v:IsA("DepthOfFieldEffect") then
                    v.Enabled = false
                end
            end

        end)

        Fluent:Notify({
            Title = "Boost",
            Content = "Low Graphics Enabled",
            Duration = 3
        })

    end
})

--// FPS BOOST

BoostTab:AddButton({
    Title = "FPS Boost",
    Description = "Boost FPS for mobile",
    Callback = function()

        pcall(function()

            settings().Rendering.QualityLevel =
                Enum.QualityLevel.Level01

            game:GetService("RunService"):Set3dRenderingEnabled(true)

            for _,v in ipairs(game:GetDescendants()) do

                if v:IsA("ParticleEmitter")
                or v:IsA("PostEffect") then
                    v.Enabled = false
                end
            end

        end)

        Fluent:Notify({
            Title = "Boost",
            Content = "FPS Boost Enabled",
            Duration = 3
        })

    end
})

--// REMOVE TEXTURES

BoostTab:AddButton({
    Title = "Remove Textures",
    Description = "Ultra FPS mode",
    Callback = function()

        for _,v in ipairs(game:GetDescendants()) do

            if v:IsA("Texture")
            or v:IsA("Decal") then
                v:Destroy()
            end
        end

        Fluent:Notify({
            Title = "Boost",
            Content = "Textures Removed",
            Duration = 3
        })

    end
})

--// DARK GRAPHICS

BoostTab:AddButton({
    Title = "Dark Graphics",
    Description = "Dark optimized graphics",
    Callback = function()

        local Lighting = game:GetService("Lighting")

        Lighting.Brightness = 0.8

        for _,v in ipairs(Lighting:GetDescendants()) do

            if v:IsA("ColorCorrectionEffect") then
                v.Saturation = -0.2
                v.Brightness = -0.05
            end
        end

        Fluent:Notify({
            Title = "Boost",
            Content = "Dark Graphics Enabled",
            Duration = 3
        })

    end
})

Window:SelectTab(InformationTab)

--// ANTI AFK (luôn để code này ở cuối) 

local Players = game:GetService("Players")
local VirtualUser = game:GetService("VirtualUser")

Players.LocalPlayer.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:Button2Down(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
    task.wait(1)
    VirtualUser:Button2Up(Vector2.new(0,0), workspace.CurrentCamera.CFrame)
end)
